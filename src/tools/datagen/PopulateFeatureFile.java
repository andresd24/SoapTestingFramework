package tools.datagen;

public class PopulateFeatureFile {
	
	// 1. Parse feature files looking for #autodatagen# tag
	// 2. Find corresponding json file for feature (e.g.  fueling.feature -> fueling.feature.json)
	// 3. Replace #autodatagen# with data from JSON files 
	// 4. Run tests (compile)
}
